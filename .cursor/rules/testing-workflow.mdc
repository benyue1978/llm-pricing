---
description: Testing workflow after code changes, including npx CLI, Playwright e2e, and Vitest unit tests
alwaysApply: true
---

# 测试工作流约定（代码改动后必须执行）

每次对功能相关代码做改动时，必须先确定验证方式，并在任务结束前完成对应测试。优先使用自动化测试，其次才是手动验证。

## 总体要求

- 优先通过命令行或自动化测试来验证改动效果，而不是只做简单的手动点点看。
- 在声称「任务完成」或准备提交代码前，必须：
  - 明确说明跑了哪些测试命令；
  - 简要描述测试结果（通过 / 失败及原因）。
- 如果因为客观原因暂时无法跑某类测试，需要在回复中说明原因，并给出后续补齐计划。

## npx CLI 程序测试

当仓库包含可通过 `npx` 调用的 CLI 工具时，对 CLI 相关改动需要：

- **优先使用已有 npm script**：
  - 示例：`npm test:cli`、`npm run cli:test`、`pnpm test:cli` 等。
  - 如果 `package.json` 中定义了更具体的 CLI 测试脚本，优先使用那些脚本。
- **如果缺少专门的 CLI 测试脚本**：
  - 使用对应的 `npx` 命令验证核心用例，例如：
    - `npx <cli-name> --help`
    - `npx <cli-name> <典型参数组合>`
  - 覆盖至少一个典型成功路径和一个重要的错误/边界情况（如果适用）。

在回答中，应明确列出实际运行的 CLI 命令及其输出结果的结论性描述。

## Playwright e2e 测试（页面端到端）

当项目中存在 Playwright 配置或 e2e 测试时，与前端页面行为、路由、交互流程相关的改动需要：

- **使用项目约定的 e2e 测试命令**，例如：
  - `npx playwright test`
  - `npm run test:e2e` / `pnpm test:e2e`
  - 如果 `package.json` 中定义了更具体的 e2e 命令（例如只跑某一条测试），可以按需选择。
- 尽量至少运行覆盖本次改动页面或流程的测试用例；如果难以精准筛选，可以接受跑全量 e2e 测试。
- 如需启动本地服务再跑 e2e，应遵循项目现有脚本和文档约定（例如先 `npm run dev` 或 `npm run start:e2e`）。

在回复中，应说明：

- 使用了哪条 Playwright/e2e 命令；
- 相关用例是否全部通过；如有失败，简要给出失败原因及是否已修复。

## Vitest 单元测试

当项目使用 Vitest 进行单元测试时，对逻辑、组件、工具函数等的改动需要：

- **优先运行受影响范围内的测试**：
  - 示例：
    - `npx vitest`（结合 `--runTestsByPath` / `--testNamePattern` 等选项，如果项目已有约定）
    - `npm run test` / `npm run test:unit` / `pnpm test:unit`
  - 可以根据项目现有脚本和目录结构，只跑与当前改动强相关的测试文件。
- 在改动较大、影响范围不明确时，建议跑一次更大范围或全量的 Vitest 测试。
- 如有相关测试文件缺失，优先补齐或至少记录成后续待办事项。

- **Provider / 价格数据源相关改动**：
  - 每次新增或修改任意 provider（如 `openai`、`anthropic`、`google`、`mistral`、`deepseek`、`qwen`、`moonshot`、`minimax`、`zhipu` 等），或调整价格拉取 / 解析逻辑时，必须运行：
    - 至少一次 `npx vitest tests/unit/providers-fetch.test.ts`（或等价命令）；
    - 或运行全量 `npm test` / `npm run test:unit`，并确认与 provider 相关的用例全部通过。
  - 在回复中需要说明这些与 provider 相关的测试是否已运行及结果。

在回复中，应说明：

- 运行了哪些 Vitest 命令（包括重要参数）；
- 测试结果是否全部通过；如有失败，大致错误类型和是否已处理。

## 验证与汇报约定

在执行涉及上述任一测试类型的开发任务时，助手在回复用户时应：

- **先列出本次执行过的测试命令**（包括 CLI / Playwright / Vitest）；
- **给出每类测试的结果简要总结**（例如「全部通过」或简要说明失败原因）；
- 如果某类测试因环境或其他限制暂时无法执行，**明确说明未执行原因**，并建议后续如何在本地或 CI 中补跑。

只有在完成与本次改动相关的测试（或明确解释为何暂时无法执行）后，才可以将任务视为「完成」。

